<h2>List of Works </h2>

<p>Find me in app/views/works/index.html.erb</p>

<section class="album">
  <h3>Albums</h3>
  <% albums = Work.where(media: "album")%>
  <% if albums.length > 0 %>
    <table class="table">
    <thead>
      <tr>
        <th scope="col">Votes</th>
        <th scope="col">Title</th>
        <th scope="col">Created By</th>
        <th scope="col">Published</th>
        <th scope="col">Upvote</th>
      </tr> 
    </thead>
    <tbody>
    <% albums.each do |album| %>
      <tr>
        <th scope="row"><%=album.upvotes%></th>
        <td><%=album.title%></td>
        <td><%=album.created_by%></td>
        <td><%=album.published%></td>
        <td><%= link_to "Upvote", vote_path(album.id), method: :post %></td>
      </tr>
      <% end %>
  <%else%>
  <% end %>
  </tbody>
</table>
</section>

<section class="books">
  <h3>Books</h3>
  <% books = Work.where(media: "book")%>
  <% if books.length > 0 %>
    <table class="table">
    <thead>
      <tr>
        <th scope="col">Votes</th>
        <th scope="col">Title</th>
        <th scope="col">Created By</th>
        <th scope="col">Published</th>
        <th scope="col">Upvote</th>
      </tr> 
    </thead>
    <tbody>
    <% books.each do |book| %>
      <tr>
        <th scope="row"><%=book.upvotes%></th>
        <td><%=book.title%></td>
        <td><%=book.created_by%></td>
        <td><%=book.published%></td>
        <td><%= link_to "Upvote", vote_path(book.id), method: :post %></td>
      </tr>
      <% end %>
  <%else%>
  <% end %>
  </tbody>
</table>
</section>

<section class="movies">
  <h3>Movies</h3>
  <% movies = Work.where(media: "movie")%>
  <% if movies.length > 0 %>
    <table class="table">
    <thead>
      <tr>
        <th scope="col">Votes</th>
        <th scope="col">Title</th>
        <th scope="col">Created By</th>
        <th scope="col">Published</th>
        <th scope="col">Upvote</th>
      </tr>
    </thead>
    <tbody>
    <% movies.each do |movie| %>
      <tr>
        <th scope="row"><%=movie.upvotes%></th>
        <td><%=movie.title%></td>
        <td><%=movie.created_by%></td>
        <td><%=movie.published%></td>
        <td><%= link_to "Upvote", vote_path(movie.id), method: :post %></td>
      </tr>
    <%end%>
  <%else%>
  <% end %>
  </tbody>
</table>
</section>
